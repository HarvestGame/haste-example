machine:
  ghc:
    version: 7.10.1

dependencies:
  cache_directories:
     - .cabal-sandbox
  pre:
     - cabal update
  override:
     - cabal sandbox init 
     - cabal install haste-compiler
     - export PATH=$PATH:.cabal-sandbox/bin
     - haste-boot

test:
  override:
    - make
